<?php
declare(strict_types=1);

use Magento\Framework\Escaper;
use Magento\Framework\View\Element\Template;

/** @var Escaper $escaper */
/** @var Template $block */
?>
<template id="ai-agent-card-comparison">
    <div x-data="initAiAgentCardComparison" class="ai-agent-card border border-container rounded-lg p-4 bg-surface overflow-x-auto">
        <p class="text-sm font-semibold mb-3" x-text="title"></p>
        <table class="w-full text-sm border-collapse">
            <thead>
                <tr>
                    <template x-for="entry in card.payload.entries" :key="entry.product_id">
                        <th class="text-left align-top p-2 border-b border-container">
                            <span x-text="entry.product.title"></span>
                            <template x-if="optionValuesText">
                                <span class="block text-xs text-secondary font-normal" x-text="optionValuesText"></span>
                            </template>
                            <template x-if="isRecommended">
                                <span class="ml-2 inline-flex items-center text-xxs uppercase rounded px-1.5 py-0.5 bg-status-successlight text-status-success">
                                    <?= $escaper->escapeHtml(__('Recommended')) ?>
                                </span>
                            </template>
                        </th>
                    </template>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <template x-for="entry in card.payload.entries" :key="entry.product_id">
                        <td class="p-2 border-b border-container font-semibold" x-text="priceOf"></td>
                    </template>
                </tr>
                <tr>
                    <template x-for="entry in card.payload.entries" :key="entry.product_id">
                        <td class="p-2 border-b border-container align-top">
                            <ul class="list-disc list-inside">
                                <template x-for="pro in prosOf">
                                    <li x-text="pro"></li>
                                </template>
                            </ul>
                        </td>
                    </template>
                </tr>
                <tr>
                    <template x-for="entry in card.payload.entries" :key="entry.product_id">
                        <td class="p-2 border-b border-container align-top">
                            <ul class="list-disc list-inside text-secondary">
                                <template x-for="con in consOf">
                                    <li x-text="con"></li>
                                </template>
                            </ul>
                        </td>
                    </template>
                </tr>
                <tr>
                    <template x-for="entry in card.payload.entries" :key="entry.product_id">
                        <td class="p-2 align-top text-xs text-secondary" x-show="hasBestFor" x-text="bestFor"></td>
                    </template>
                </tr>
            </tbody>
        </table>
        <template x-if="card.payload.price_delta">
            <p class="text-xs text-secondary mt-3" x-text="priceDeltaLine"></p>
        </template>
    </div>
</template>
