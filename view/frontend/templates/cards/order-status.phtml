<?php
declare(strict_types=1);

use Magento\Framework\Escaper;
use Magento\Framework\View\Element\Template;

/** @var Escaper $escaper */
/** @var Template $block */
?>
<template id="ai-agent-card-order-status">
    <div x-data="initAiAgentCardOrderStatus" class="ai-agent-card border border-container rounded-lg p-4 bg-surface">
        <span
            class="inline-flex items-center text-xxs uppercase rounded px-2 py-1"
            :class="statusClass"
            x-text="statusLabel"
        ></span>
        <p class="text-sm mt-2" x-text="card.payload.summary"></p>
        <template x-if="card.payload.next_step">
            <p class="text-xs text-secondary mt-1" x-text="card.payload.next_step"></p>
        </template>
        <ul class="mt-3 space-y-1">
            <template x-for="item in card.payload.order.items" :key="item.product_id">
                <li class="flex justify-between gap-2 text-xs">
                    <span x-text="item.title"></span>
                    <span x-text="item.quantity"></span>
                </li>
            </template>
        </ul>
        <template x-if="card.payload.order.tracking_url">
            <a :href="card.payload.order.tracking_url" class="btn btn-secondary btn-sm mt-3 inline-block">
                <?= $escaper->escapeHtml(__('Track shipment')) ?>
            </a>
        </template>
    </div>
</template>
